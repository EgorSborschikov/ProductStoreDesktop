<Page x:Class="ProductStoreDesktop.View.Pages.CartPage"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
      xmlns:local="clr-namespace:ProductStoreDesktop.View.Pages"
      mc:Ignorable="d" 
      d:DesignHeight="450" d:DesignWidth="800"
      Title="Корзина">

    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <TextBlock x:Name="CartSummaryText" 
                   Grid.Row="0" 
                   FontSize="16" 
                   FontWeight="Bold" 
                   Margin="0,0,0,20"/>

        <ListView x:Name="CartItemsListView" Grid.Row="1"
                  d:ItemsSource="{d:SampleData ItemCount=3}">
            <ListView.View>
                <GridView>
                    <GridViewColumn Header="Товар" Width="250">
                        <GridViewColumn.CellTemplate>
                            <DataTemplate>
                                <StackPanel Orientation="Horizontal">
                                    <Image Source="{Binding ImageUrl}" Width="50" Height="50" Stretch="Uniform"/>
                                    <TextBlock Text="{Binding Name}" Margin="10,0,0,0" VerticalAlignment="Center"/>
                                </StackPanel>
                            </DataTemplate>
                        </GridViewColumn.CellTemplate>
                    </GridViewColumn>
                    <GridViewColumn Header="Цена" Width="100" 
                                    DisplayMemberBinding="{Binding Price, StringFormat={}{0:C}}"/>
                    <GridViewColumn Header="Кол-во" Width="140">
                        <GridViewColumn.CellTemplate>
                            <DataTemplate>
                                <StackPanel Orientation="Horizontal" VerticalAlignment="Center">
                                    <Button Content="-" Width="25" Tag="{Binding Id}" Click="DecreaseQuantity_Click"/>
                                    <TextBlock Text="{Binding Quantity}" Width="30" TextAlignment="Center"/>
                                    <Button Content="+" Width="25" Tag="{Binding Id}" Click="IncreaseQuantity_Click"/>
                                </StackPanel>
                            </DataTemplate>
                        </GridViewColumn.CellTemplate>
                    </GridViewColumn>
                    <GridViewColumn Header="Итого" Width="100" 
                                    DisplayMemberBinding="{Binding TotalPrice, StringFormat={}{0:C}}"/>
                    <GridViewColumn Header="Действие" Width="100">
                        <GridViewColumn.CellTemplate>
                            <DataTemplate>
                                <Button Content="Удалить" Tag="{Binding Id}" Click="RemoveFromCart_Click"/>
                            </DataTemplate>
                        </GridViewColumn.CellTemplate>
                    </GridViewColumn>
                </GridView>
            </ListView.View>
        </ListView>

        <Button x:Name="CheckoutButton" 
                Grid.Row="2" 
                Content="Оформить заказ"
                HorizontalAlignment="Right" 
                Padding="20,10" 
                FontSize="14"
                Click="Checkout_Click"
                Margin="0,20,0,0"/>
    </Grid>
</Page>
